spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/comeet?serverTimezone=UTC # mysql 8.2, mysql workbench 8.0.21 / mac은 8.0.34
    username: comeet
    password: comeet
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
#    show-sql: true

logging.level:
  org.hibernate.SQL: debug