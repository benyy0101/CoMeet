import React from "react";

import tw from "tailwind-styled-components";

export const MyKeyword = () => {
  return <TotalContainer>MyKeyword - 워드클라우드 API</TotalContainer>;
};

//전체 컨테이너
const TotalContainer = tw.div`
w-full
h-full
text-white
`;
