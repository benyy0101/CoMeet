import React, { Dispatch, SetStateAction } from "react";

const RoomChannel: React.FC<{}> = (props) => {
  return <div>RoomChannel</div>;
};

//채널 안에서 x 버튼 누르면 false로 변하고 room/:roomId로 가야함

export default RoomChannel;
